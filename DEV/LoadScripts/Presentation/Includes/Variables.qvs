/* This script contains all variables used in our app's visualizations

   Use this instead of leaving them hardcoded in the QVF file.
 */
// Load standard colour variables.
$(MUST_INCLUDE=[lib://$(vScriptFiles)/$(vIncludesFolder)/Variables/ColorVariables.qvs]);
$(MUST_INCLUDE=[lib://$(vScriptFiles)/$(vIncludesFolder)/Variables/Variables.qvs]);
